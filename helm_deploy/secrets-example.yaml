# This is a YAML-formatted file.
# 
# Template for secrets which will be stored in AWS secret manager

secrets:
  API_AUTH: secret_value 
  GOVUK_NOTIFY_API_KEY: secret_value 
  MOJSSO_URL: secret_value 
  MOJSSO_ID: secret_value 
  MOJSSO_SECRET: secret_value 
  RECAPTCHA_SECRET_KEY: secret_value
  RECAPTCHA_SITE_KEY: secret_value
  SECRET_KEY_BASE: secret_value 
